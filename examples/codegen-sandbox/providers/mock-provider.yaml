apiVersion: promptkit.altairalabs.ai/v1alpha1
kind: Provider
metadata:
  name: mock-codegen
spec:
  id: mock-codegen
  type: mock
  model: mock-model
  additional_config:
    mock_config: mock-responses.yaml
  defaults:
    temperature: 0.1
    max_tokens: 512
    top_p: 1.0
