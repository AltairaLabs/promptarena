# @altairalabs/* live in GitHub Packages (private). NODE_AUTH_TOKEN needs
# read:packages — CI supplies it; locally, export a PAT before installing.
@altairalabs:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
