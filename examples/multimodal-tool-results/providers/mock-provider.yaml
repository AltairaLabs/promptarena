apiVersion: promptkit.altairalabs.ai/v1alpha1
kind: Provider
metadata:
  name: mock-provider
spec:
  id: mock-provider
  type: mock
  model: mock-model
  additional_config:
    mock_config: mock-responses.yaml
