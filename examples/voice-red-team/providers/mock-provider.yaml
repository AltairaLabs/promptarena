apiVersion: promptkit.altairalabs.ai/v1alpha1
kind: Provider
metadata:
  name: mock-redteam
spec:
  id: mock-redteam
  type: mock
  model: mock-model
  additional_config:
    mock_config: mock-responses.yaml
  defaults:
    temperature: 0.1
    max_tokens: 256
    top_p: 1.0
