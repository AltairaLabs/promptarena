apiVersion: promptkit.altairalabs.ai/v1alpha1
kind: Arena
metadata:
  name: interactive-idvars-fixture
spec:
  prompt_configs:
    - id: support-cfg
      file: prompts/support.yaml
      vars:
        company: FixtureCo
  providers:
    - file: providers/mock.provider.yaml
  defaults:
    temperature: 0.5
    max_tokens: 100
