apiVersion: promptkit.altairalabs.ai/v1alpha1
kind: Scenario
metadata:
  name: refund-broken-mock
  labels:
    use_case: support
    complexity: simple
spec:
  id: refund-broken-mock
  task_type: authoring-agent
  description: "Mock wiring check: scripted agent scaffolds a deliberately broken kit; the shared gates must FAIL — proving the gates discriminate, not rubber-stamp."
  turns:
    - role: user
      content: "Scaffold a minimal refund kit under /workspace/kit."
