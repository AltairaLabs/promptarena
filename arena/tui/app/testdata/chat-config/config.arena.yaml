apiVersion: promptkit.altairalabs.ai/v1alpha1
kind: Arena
metadata:
  name: chat-test-config
  description: Fixture for ChatPage unit tests
spec:
  prompt_configs:
    - id: basic
      file: prompts/basic.yaml
  providers:
    - file: providers/mock.provider.yaml
  defaults:
    temperature: 0.5
    max_tokens: 100
