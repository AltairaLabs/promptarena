apiVersion: promptkit.altairalabs.ai/v1alpha1
kind: PromptConfig
metadata:
  name: assistant
spec:
  version: "1.0.0"
  description: "Assistant prompt configuration for helpful AI responses"
  task_type: "assistant"
  system_template: "You are a helpful AI assistant. Be concise and direct in your responses."
  allowed_tools:
    - search
    - calculate
