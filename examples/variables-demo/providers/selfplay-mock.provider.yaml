apiVersion: promptkit.altairalabs.ai/v1alpha1
kind: Provider
metadata:
  name: selfplay-mock
spec:
  id: selfplay-mock
  type: mock
  model: mock-model
  defaults:
    temperature: 0.7
    max_tokens: 1000
    top_p: 1.0
  additional_config:
    mock_config: "mock-config-selfplay.yaml"
