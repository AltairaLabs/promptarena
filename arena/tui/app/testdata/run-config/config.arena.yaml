apiVersion: promptkit.altairalabs.ai/v1alpha1
kind: Arena
metadata:
  name: run-test-config
  description: Fixture for RunPage unit tests
spec:
  prompt_configs:
    - id: basic
      file: prompts/basic.yaml
  providers:
    - file: providers/mock.provider.yaml
  scenarios:
    - file: scenarios/greeting.scenario.yaml
  defaults:
    temperature: 0.5
    max_tokens: 100
    seed: 42
