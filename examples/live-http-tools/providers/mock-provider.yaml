apiVersion: promptkit.altairalabs.ai/v1alpha1
kind: Provider
metadata:
  name: mock-live-tools
spec:
  id: mock-live-tools
  type: mock
  model: mock-model
  additional_config:
    mock_config: mock-responses.yaml
